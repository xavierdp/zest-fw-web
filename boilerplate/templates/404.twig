{% extends 'base.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="text-center py-10">
        <h2 class="text-5xl font-bold text-red-600 mb-4">404</h2>
        <h3 class="text-2xl font-semibold mb-6">Page Not Found</h3>
        <p class="text-gray-600 mb-8">The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.</p>
        
        <a href="{{ url('') }}" class="inline-block bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition">
            Return to Homepage
        </a>
    </div>
{% endblock %}

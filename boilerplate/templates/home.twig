{% extends 'base.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="bg-white shadow-md rounded-lg p-6">
        <h2 class="text-3xl font-bold text-blue-600 mb-4">{{ title }}</h2>
        <p class="text-gray-700 mb-4">{{ content }}</p>
        
        <div class="mt-8 bg-gray-100 p-4 rounded-md">
            <h3 class="text-xl font-semibold mb-2">Getting Started</h3>
            <p class="mb-2">This is a sample application built with ZestPHP Web Framework.</p>
            <ul class="list-disc pl-5 space-y-1">
                <li>Edit templates in the <code class="bg-gray-200 px-1 rounded">templates/</code> directory</li>
                <li>Add static files (CSS, JS, images) to <code class="bg-gray-200 px-1 rounded">webroot/static/</code></li>
                <li>Define routes in <code class="bg-gray-200 px-1 rounded">webroot/index.php</code></li>
            </ul>
        </div>
        
        <div class="mt-6">
            <a href="{{ url('api/hello') }}" class="inline-block bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition">
                Try the API
            </a>
        </div>
        
        <div class="mt-8 border-t pt-6">
            <h3 class="text-xl font-semibold mb-4">Component System Demo</h3>
            
            <div class="space-y-4">
                <div>
                    <h4 class="text-lg font-medium mb-2">Button Component</h4>
                    <p class="text-gray-600 mb-3">The Button component can be used with different styles and sizes:</p>
                    
                    <div class="flex flex-wrap gap-2 mb-4">
                        {{ component('Button', {text: 'Primary Button', type: 'primary'}) }}
                        {{ component('Button', {text: 'Secondary Button', type: 'secondary'}) }}
                        {{ component('Button', {text: 'Success Button', type: 'success'}) }}
                        {{ component('Button', {text: 'Danger Button', type: 'danger'}) }}
                    </div>
                    
                    <div class="flex flex-wrap gap-2">
                        {{ component('Button', {text: 'Small Button', size: 'sm'}) }}
                        {{ component('Button', {text: 'Medium Button', size: 'md'}) }}
                        {{ component('Button', {text: 'Large Button', size: 'lg'}) }}
                        {{ component('Button', {text: 'Disabled Button', disabled: true}) }}
                    </div>
                </div>
                
                <div>
                    <h4 class="text-lg font-medium mb-2">Using Components in Templates</h4>
                    <p class="text-gray-600 mb-3">Components can be easily included in any template using the <code class="bg-gray-200 px-1 rounded">component()</code> function:</p>
                    
                    <div class="bg-gray-800 text-gray-100 p-4 rounded font-mono text-sm">
                        <pre>{{ '{{ component(\'Button\', {text: \'Click Me\', type: \'primary\'}) }}' }}</pre>
                    </div>
                </div>
                
                <div>
                    <h4 class="text-lg font-medium mb-2">Component Structure</h4>
                    <p class="text-gray-600 mb-3">Each component consists of four files:</p>
                    
                    <ul class="list-disc pl-5 space-y-1">
                        <li><code class="bg-gray-200 px-1 rounded">ComponentName.php</code> - Component logic</li>
                        <li><code class="bg-gray-200 px-1 rounded">ComponentName.twig</code> - Component template</li>
                        <li><code class="bg-gray-200 px-1 rounded">ComponentName.js</code> - Component JavaScript</li>
                        <li><code class="bg-gray-200 px-1 rounded">ComponentName.css</code> - Component styles</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
